# Termux-specific requirements
# Binary packages are installed via pkg instead of pip where possible

fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.9.2  # Specific version with confirmed ARM binary wheels
pydantic-core==2.9.0  # Core with ARM binary wheels
pydantic-settings==2.1.0
# cryptography installed via: pkg install python-cryptography
# bcrypt installed via: pkg install python-bcrypt
aiosqlite==0.20.0
python-multipart==0.0.20
psutil==6.1.1
mnemonic==0.21
structlog==24.1.0
prometheus-client==0.19.0
httpx==0.28.1  # HTTP client for LLM API backends

# Note: All LLM backends (Anthropic, OpenAI, HuggingFace) work via simple HTTP requests
# No anthropic package needed - we use httpx directly (no Rust/tokenizers dependency)
