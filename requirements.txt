fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.5
pydantic-settings==2.1.0  # For environment-based configuration
cryptography==44.0.0
pynacl==1.5.0
aiosqlite==0.20.0
python-multipart==0.0.20
bcrypt==4.1.2
psutil==6.1.1
mnemonic==0.21  # BIP39 seed phrase generation and derivation
structlog==24.1.0  # Structured logging for better observability
prometheus-client==0.19.0  # Prometheus metrics collection (GAP-54)

# Agent system dependencies
# Use --only-binary to avoid building tokenizers from source (requires Rust/maturin)
# Install with: pip install --only-binary :all: anthropic==0.18.0
anthropic==0.18.0  # Claude API for LLM-powered reasoning (optional)
