/**
 * Attestation Claim Page - DISABLED
 * 
 * This page requires Material-UI dependencies that aren't installed.
 * Temporarily disabled to fix build issues.
 */

export default function AttestationClaimPage() {
  return (
    <div style={{ padding: '2rem', textAlign: 'center' }}>
      <h1>Attestation Claim Page</h1>
      <p>This page is temporarily disabled due to missing Material-UI dependencies.</p>
      <p>Please install @mui/material and @mui/icons-material to enable this feature.</p>
    </div>
  );
}
