# Discovery and Search System Requirements

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
aiosqlite>=0.19.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Existing dependencies from DTN Bundle System
# These should already be installed:
# - cryptography (for Ed25519 signing)
# - python-multipart (for FastAPI)
